node_modules/
dist/
*.db
*.db-journal
.env
/scans/
# TLS interception CA certs exported for NODE_EXTRA_CA_CERTS -- these are
# specific to one machine's corporate security software (see README), not
# something to version or share.
*.pem
*.cer
